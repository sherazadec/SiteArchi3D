@model IEnumerable<SiteArchi3D.Modélisateurs>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Personnes.Nom)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Personnes.Nom)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idModélisateur }) |
            @Html.ActionLink("Details", "Details", new { id=item.idModélisateur }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idModélisateur })
        </td>
    </tr>
}

</table>
